# Databricks notebook source
# DBTITLE 1,Header
# MAGIC %md
# MAGIC # ðŸš€ Claude Sonnet 4.6 â€” UPSC Superpowers Demo
# MAGIC
# MAGIC **Model Strategy:** NB6 CA Pipeline â†’ **Opus 4.6** (deep reasoning) | Everything else â†’ **Sonnet 4.6** (90% quality, 3x cheaper)
# MAGIC
# MAGIC This notebook demonstrates everything you can do with Claude Sonnet 4.6 on your UPSC data. Each cell is a standalone example you can copy into any notebook or pipeline.
# MAGIC
# MAGIC **Endpoints used:** `databricks-claude-sonnet-4-6` (input: 42.857 DBU/1M, output: 214.286 DBU/1M tokens)
# MAGIC
# MAGIC ---

# COMMAND ----------

# DBTITLE 1,ðŸ“° CA Story Deep Analysis
# MAGIC %sql
# MAGIC -- Pick the latest CA story and generate a comprehensive UPSC Mains analysis using Sonnet 4.6
# MAGIC SELECT
# MAGIC   s.title,
# MAGIC   s.date,
# MAGIC   s.gs_papers,
# MAGIC   s.topic_cluster,
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC CSE Mains expert. Analyze this Current Affairs story for UPSC relevance:\n\n',
# MAGIC       'Title: ', s.title, '\n',
# MAGIC       'Topic: ', s.topic_cluster, '\n',
# MAGIC       'GS Papers: ', COALESCE(s.gs_papers, 'N/A'), '\n',
# MAGIC       'Keywords: ', COALESCE(s.keywords, 'N/A'), '\n\n',
# MAGIC       'Provide:\n1. GS Paper Mapping (which papers & why)\n2. Key UPSC Keywords (5-7)\n3. 250-word Model Answer suitable for Mains GS\n4. Probable Prelims angle (1 MCQ stem)'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 1000, 'temperature', 0.3)
# MAGIC   ) AS sonnet_analysis
# MAGIC FROM upsc_catalog.rag.stories s
# MAGIC ORDER BY s.date DESC
# MAGIC LIMIT 1

# COMMAND ----------

# DBTITLE 1,ðŸŽ¯ Auto-Generate Prelims MCQs from Polity
# MAGIC %sql
# MAGIC -- Generate 3 UPSC Prelims-style MCQs from Polity chunks using Sonnet 4.6
# MAGIC WITH polity_chunks AS (
# MAGIC   SELECT chunk_id, text, source_file
# MAGIC   FROM upsc_catalog.rag.contextual_chunks
# MAGIC   WHERE subject = 'Polity'
# MAGIC   ORDER BY RAND()
# MAGIC   LIMIT 3
# MAGIC )
# MAGIC SELECT
# MAGIC   pc.source_file,
# MAGIC   SUBSTRING(pc.text, 1, 100) AS chunk_preview,
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC Prelims question setter. From this passage, create 1 MCQ:\n\n',
# MAGIC       pc.text, '\n\n',
# MAGIC       'Format:\nQ: [Question]\n(a) ...\n(b) ...\n(c) ...\n(d) ...\nAnswer: (x)\nExplanation: [Why this is correct and others are wrong, cite specific facts]'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 500, 'temperature', 0.5)
# MAGIC   ) AS generated_mcq
# MAGIC FROM polity_chunks pc

# COMMAND ----------

# DBTITLE 1,ðŸ”— Knowledge Graph â†’ Mains Interlinking
# MAGIC %sql
# MAGIC -- Find the most connected entity in KG and generate an interlinking Mains answer
# MAGIC WITH top_entity AS (
# MAGIC   SELECT
# MAGIC     e.entity_name,
# MAGIC     e.entity_type,
# MAGIC     e.description,
# MAGIC     COUNT(*) AS connection_count
# MAGIC   FROM upsc_catalog.rag.kg_entities e
# MAGIC   JOIN upsc_catalog.rag.kg_relationships r
# MAGIC     ON e.entity_id = r.source_entity_id OR e.entity_id = r.target_entity_id
# MAGIC   GROUP BY e.entity_name, e.entity_type, e.description
# MAGIC   ORDER BY connection_count DESC
# MAGIC   LIMIT 1
# MAGIC ),
# MAGIC entity_connections AS (
# MAGIC   SELECT
# MAGIC     te.entity_name,
# MAGIC     te.entity_type,
# MAGIC     te.description,
# MAGIC     te.connection_count,
# MAGIC     COLLECT_LIST(
# MAGIC       CONCAT(r.relationship_type, ' â†’ ', e2.entity_name, ' (', e2.entity_type, ')')
# MAGIC     ) AS connections
# MAGIC   FROM top_entity te
# MAGIC   JOIN upsc_catalog.rag.kg_relationships r
# MAGIC     ON EXISTS (SELECT 1 FROM upsc_catalog.rag.kg_entities e WHERE e.entity_name = te.entity_name AND (e.entity_id = r.source_entity_id OR e.entity_id = r.target_entity_id))
# MAGIC   JOIN upsc_catalog.rag.kg_entities e2
# MAGIC     ON (e2.entity_id = r.target_entity_id OR e2.entity_id = r.source_entity_id) AND e2.entity_name != te.entity_name
# MAGIC   GROUP BY te.entity_name, te.entity_type, te.description, te.connection_count
# MAGIC )
# MAGIC SELECT
# MAGIC   ec.entity_name,
# MAGIC   ec.entity_type,
# MAGIC   ec.connection_count,
# MAGIC   SLICE(ec.connections, 1, 10) AS top_connections,
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC Mains interlinking expert.\n\n',
# MAGIC       'Entity: ', ec.entity_name, ' (', ec.entity_type, ')\n',
# MAGIC       'Description: ', COALESCE(ec.description, 'N/A'), '\n',
# MAGIC       'Connected to: ', ARRAY_JOIN(SLICE(ec.connections, 1, 10), '; '), '\n\n',
# MAGIC       'Write a 200-word answer demonstrating how this entity connects across multiple GS papers (GS1-GS4). ',
# MAGIC       'Show cross-paper interlinking that would impress a UPSC examiner.'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 800, 'temperature', 0.4)
# MAGIC   ) AS interlinking_answer
# MAGIC FROM entity_connections ec

# COMMAND ----------

# DBTITLE 1,ðŸ“ Telugu Optional â€” AI Evaluation
# MAGIC %sql
# MAGIC -- Generate a PYQ-style question + model answer with Telugu rubric using Sonnet 4.6
# MAGIC SELECT
# MAGIC   cc.source_file,
# MAGIC   cc.doc_type,
# MAGIC   SUBSTRING(cc.text, 1, 150) AS chunk_preview,
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a Telugu Optional UPSC examiner. Based on this Telugu literature passage:\n\n',
# MAGIC       SUBSTRING(cc.text, 1, 2000), '\n\n',
# MAGIC       'Generate:\n1. A PYQ-style question (in English, about this Telugu literary topic)\n',
# MAGIC       '2. A model answer (250 words) covering:\n',
# MAGIC       '   - Content depth (40% weightage)\n',
# MAGIC       '   - Language & expression quality (30% weightage)\n',
# MAGIC       '   - Presentation & structure (20% weightage)\n',
# MAGIC       '   - Originality of analysis (10% weightage)\n',
# MAGIC       '3. Examiner tips: what would score 40+ out of 50'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 1000, 'temperature', 0.4)
# MAGIC   ) AS telugu_evaluation
# MAGIC FROM upsc_catalog.rag.contextual_chunks cc
# MAGIC WHERE cc.subject = 'Telugu Optional'
# MAGIC   AND cc.doc_type != 'PYQ'
# MAGIC ORDER BY RAND()
# MAGIC LIMIT 1

# COMMAND ----------

# DBTITLE 1,âš ï¸ Trap Detector â€” Weekly Trap Flashcards
# MAGIC %sql
# MAGIC -- Analyze this week's traps and generate counter-strategy flashcards using Sonnet 4.6
# MAGIC WITH weekly_traps AS (
# MAGIC   SELECT
# MAGIC     t.trap_type,
# MAGIC     t.subject,
# MAGIC     t.wrong_belief,
# MAGIC     t.correct_belief,
# MAGIC     t.severity,
# MAGIC     s.title AS story_title
# MAGIC   FROM upsc_catalog.rag.story_traps t
# MAGIC   JOIN upsc_catalog.rag.stories s
# MAGIC     ON t.story_slug = s.slug AND t.created_date = s.date
# MAGIC   WHERE t.created_date >= date_sub(current_date(), 7)
# MAGIC   ORDER BY CASE t.severity WHEN 'HIGH' THEN 1 WHEN 'MEDIUM' THEN 2 WHEN 'LOW' THEN 3 ELSE 4 END
# MAGIC   LIMIT 5
# MAGIC )
# MAGIC SELECT
# MAGIC   wt.subject,
# MAGIC   wt.severity,
# MAGIC   wt.story_title,
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC Prelims trap-buster.\n\n',
# MAGIC       'Subject: ', wt.subject, '\n',
# MAGIC       'Trap Type: ', wt.trap_type, '\n',
# MAGIC       'Wrong Belief: ', wt.wrong_belief, '\n',
# MAGIC       'Correct Belief: ', wt.correct_belief, '\n',
# MAGIC       'Severity: ', wt.severity, '\n',
# MAGIC       'Source Story: ', wt.story_title, '\n\n',
# MAGIC       'Generate:\n',
# MAGIC       '1. FLASHCARD: Wrong â†’ Correct (with mnemonic trick)\n',
# MAGIC       '2. MCQ designed to TEST this exact trap\n',
# MAGIC       '3. Related traps to watch out for in same subject'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 600, 'temperature', 0.4)
# MAGIC   ) AS trap_buster
# MAGIC FROM weekly_traps wt

# COMMAND ----------

# DBTITLE 1,ðŸ“Š Essay Thread Evolution â€” Predicted 2027 Topics
# MAGIC %sql
# MAGIC -- Analyze trending essay themes and predict 2027 essay topics using Sonnet 4.6
# MAGIC WITH top_themes AS (
# MAGIC   SELECT theme, frequency, essay_title, story_ids, week_date
# MAGIC   FROM upsc_catalog.rag.essay_threads
# MAGIC   ORDER BY frequency DESC
# MAGIC   LIMIT 8
# MAGIC )
# MAGIC SELECT
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC Essay Paper prediction expert.\n\n',
# MAGIC       'These essay themes are trending based on recent Current Affairs:\n',
# MAGIC       (SELECT ARRAY_JOIN(COLLECT_LIST(
# MAGIC         CONCAT('- ', theme, ' (frequency=', CAST(frequency AS STRING), '): ', essay_title)
# MAGIC       ), '\n') FROM top_themes),
# MAGIC       '\n\nFor UPSC Essay Paper (2 essays Ã— 125 marks Ã— 3 hours):\n',
# MAGIC       '1. Predict TOP 5 most likely essay topics for 2027\n',
# MAGIC       '2. For each: give a philosophical angle + practical angle\n',
# MAGIC       '3. Suggest 2 quotes per topic (1 Indian thinker, 1 Western)\n',
# MAGIC       '4. Which 2 topics pair best together in the exam?\n',
# MAGIC       '5. One dark-horse topic nobody expects'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 1500, 'temperature', 0.5)
# MAGIC   ) AS essay_predictions

# COMMAND ----------

# DBTITLE 1,ðŸŽ“ Mains Answer Generator â€” RAG + Sonnet
# MAGIC %sql
# MAGIC -- Full RAG pipeline: retrieve chunks from KB â†’ generate exam-quality Mains answer with Sonnet 4.6
# MAGIC WITH relevant_chunks AS (
# MAGIC   SELECT text, subject, source_file
# MAGIC   FROM upsc_catalog.rag.contextual_chunks
# MAGIC   WHERE LOWER(text) LIKE '%fundamental rights%'
# MAGIC     AND LOWER(text) LIKE '%judiciary%'
# MAGIC   LIMIT 5
# MAGIC )
# MAGIC SELECT
# MAGIC   ai_query(
# MAGIC     'databricks-claude-sonnet-4-6',
# MAGIC     CONCAT(
# MAGIC       'You are a UPSC Mains answer writing coach (GS2 expert).\n\n',
# MAGIC       'REFERENCE MATERIAL FROM KNOWLEDGE BASE:\n',
# MAGIC       (SELECT ARRAY_JOIN(COLLECT_LIST(
# MAGIC         CONCAT('[', subject, ' â€” ', source_file, ']: ', SUBSTRING(text, 1, 400))
# MAGIC       ), '\n\n') FROM relevant_chunks),
# MAGIC       '\n\n---\nQUESTION: "Discuss the role of judiciary in protecting fundamental rights. How has judicial activism expanded the scope of Article 21?" (250 words, 15 marks)\n\n',
# MAGIC       'Write a complete Mains answer following this structure:\n',
# MAGIC       '1. INTRODUCTION: Context-setting, constitutional basis (2-3 lines)\n',
# MAGIC       '2. BODY: 4 subheadings with specific Article numbers, landmark cases, recent judgments\n',
# MAGIC       '3. RECENT DEVELOPMENTS: Last 2 years\' judicial actions\n',
# MAGIC       '4. CONCLUSION: Forward-looking, balanced\n',
# MAGIC       '5. EXAMINER NOTES: What makes this answer score 12+/15'
# MAGIC     ),
# MAGIC     modelParameters => named_struct('max_tokens', 1500, 'temperature', 0.3)
# MAGIC   ) AS mains_answer

# COMMAND ----------

# DBTITLE 1,ðŸ“± Obsidian Copilot + Databricks Claude Setup
# MAGIC %md
# MAGIC ---
# MAGIC # ðŸ“± Obsidian Copilot + Databricks Claude â€” Complete Setup Guide
# MAGIC
# MAGIC > **Goal:** Chat with your Obsidian vault notes using Opus 4.6 / Sonnet 4.6 powered by your Databricks endpoint. No Anthropic API key needed.
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Prerequisites
# MAGIC | What | Details |
# MAGIC |---|---|
# MAGIC | Obsidian | Installed on Mac (â‰¥ v1.4) |
# MAGIC | Vault | `~/Desktop/UPSC_2026` (synced from Databricks Volume) |
# MAGIC | Databricks PAT | Generate from: Workspace â†’ User Settings â†’ Developer â†’ Access Tokens |
# MAGIC | Internet | Must reach `adb-7405615460529826.6.azuredatabricks.net` |
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 1: Generate Databricks PAT Token
# MAGIC 1. Open your Databricks workspace in browser
# MAGIC 2. Click your profile icon (top-right) â†’ **Settings**
# MAGIC 3. Go to **Developer** â†’ **Access Tokens**
# MAGIC 4. Click **Generate New Token**
# MAGIC 5. Name: `obsidian-copilot`, Lifetime: 90 days
# MAGIC 6. **Copy the token** (starts with `dapi...`) â€” you won't see it again!
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 2: Install Copilot Plugin
# MAGIC 1. Open Obsidian â†’ Settings (âš™ï¸) â†’ **Community Plugins**
# MAGIC 2. Turn off **Restricted Mode** if prompted
# MAGIC 3. Click **Browse** â†’ Search: **"Copilot"**
# MAGIC 4. Install **Copilot** by Logan Yang (â­ most popular AI chat plugin)
# MAGIC 5. Click **Enable**
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 3: Configure Custom Model (Databricks Claude)
# MAGIC 1. Settings â†’ **Copilot** â†’ scroll to **Model Settings**
# MAGIC 2. Click **Add Custom Model** and set:
# MAGIC
# MAGIC | Setting | Value |
# MAGIC |---|---|
# MAGIC | **Display Name** | `Claude Opus 4.6 (UPSC)` |
# MAGIC | **Provider** | `OpenAI-compatible` |
# MAGIC | **Base URL** | `https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints` |
# MAGIC | **API Key** | `dapi-xxxxxxx` (your PAT from Step 1) |
# MAGIC | **Model Name** | `databricks-claude-opus-4-6` |
# MAGIC
# MAGIC 3. Optionally add a second model:
# MAGIC
# MAGIC | Setting | Value |
# MAGIC |---|---|
# MAGIC | **Display Name** | `Claude Sonnet 4.6 (Fast)` |
# MAGIC | **Model Name** | `databricks-claude-sonnet-4-6` |
# MAGIC | (rest same as above) | |
# MAGIC
# MAGIC 4. Set **Default Model** to `Claude Opus 4.6 (UPSC)` for deep analysis
# MAGIC 5. Click **Test Connection** â†’ should show a response
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 4: Configure Copilot Behavior
# MAGIC | Setting | Recommended Value | Why |
# MAGIC |---|---|---|
# MAGIC | Temperature | `0.3` | Factual for UPSC |
# MAGIC | Max Tokens | `2000` | Enough for full answers |
# MAGIC | Use Active Note as Context | âœ… ON | Sends current note to Claude |
# MAGIC | System Prompt | `You are a UPSC CSE 2027 expert tutor. Be precise, cite Article numbers, case laws, and specific facts. Format for exam answers.` | UPSC-focused |
# MAGIC | Auto-complete | âŒ OFF | Saves tokens (costs per keystroke) |
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 5: Starter Prompts (Copy-Paste Ready)
# MAGIC
# MAGIC ### ðŸ“° For CA Notes (Daily)
# MAGIC ```
# MAGIC Summarize this CA note for GS2 Mains in 150 words
# MAGIC What Prelims MCQs can be framed from this story?
# MAGIC Identify 3 constitutional articles relevant to this note
# MAGIC How does this connect to last month's CA on similar topics?
# MAGIC ```
# MAGIC
# MAGIC ### ðŸ“ For Revision
# MAGIC ```
# MAGIC Create 5 flashcards from this note (front: question, back: answer)
# MAGIC What are the 7 key facts I MUST remember from this?
# MAGIC Generate 3 PYQ-style questions from this content
# MAGIC Explain this topic as if teaching to a 10-year-old
# MAGIC ```
# MAGIC
# MAGIC ### ðŸŽ¯ For Mains Practice
# MAGIC ```
# MAGIC Write a 250-word model answer based on this note (15 marks)
# MAGIC Frame an Ethics GS4 case study from this current affairs
# MAGIC Create a mind-map structure for this topic
# MAGIC What interlinking points connect GS1, GS2, and GS3 here?
# MAGIC ```
# MAGIC
# MAGIC ### ðŸ“š For Telugu Optional
# MAGIC ```
# MAGIC Explain the literary significance of this kavya section
# MAGIC What prosody rules (chhandas) are used in these verses?
# MAGIC Compare this with similar works in Sanskrit/Kannada literature
# MAGIC How would this score as a 50-mark answer? Improve it.
# MAGIC ```
# MAGIC
# MAGIC ### âœï¸ For Essay Prep
# MAGIC ```
# MAGIC Suggest 3 essay angles from this current affairs piece
# MAGIC Find philosophical quotes that connect to this theme
# MAGIC Write an essay introduction hook for this topic
# MAGIC What would Ambedkar, Gandhi, and Kalam say about this?
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Step 6: Pro Tips
# MAGIC * **Tag your notes** with #GS1, #GS2, #GS3, #GS4, #Prelims, #Telugu â€” Copilot uses these as context
# MAGIC * **Cost control:** Sonnet 4.6 for daily use (âˆ¼2 DBU per session), Opus only for deep review
# MAGIC * **Vault-wide search:** Type `@note:` in chat to reference specific notes by name
# MAGIC * **Chain prompts:** First ask "Summarize" then "Now generate MCQs from this summary"
# MAGIC * **Offline fallback:** Copilot needs internet; use your synced Obsidian notes directly for offline review
# MAGIC * **Keyboard shortcut:** Set `Cmd+Shift+C` â†’ Open Copilot Chat for quick access
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Cost Estimate
# MAGIC | Usage Pattern | Model | DBU/Session | Monthly (âˆ¼30 sessions) |
# MAGIC |---|---|---|---|
# MAGIC | Quick Q&A (5 prompts) | Sonnet 4.6 | âˆ¼0.5 DBU | âˆ¼15 DBU |
# MAGIC | Deep Review (20 prompts) | Opus 4.6 | âˆ¼5 DBU | âˆ¼150 DBU |
# MAGIC | Recommended Mix | Sonnet daily + Opus weekend | âˆ¼1.5 DBU avg | âˆ¼45 DBU |

# COMMAND ----------

# DBTITLE 1,ðŸ’» CLI Commands Reference â€” From Scratch
# MAGIC %md
# MAGIC ---
# MAGIC # ðŸ’» CLI Commands Reference â€” All Methods, From Scratch
# MAGIC
# MAGIC > **Default Model:** `databricks-claude-sonnet-4-6` for CLI | `databricks-claude-opus-4-6` for NB6 only
# MAGIC >
# MAGIC > **Workspace:** `https://adb-7405615460529826.6.azuredatabricks.net`
# MAGIC >
# MAGIC > **Profile:** `upsc` (Mac) | `DEFAULT` (Windows VM)
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 0ï¸âƒ£ One-Time Setup (if starting fresh)
# MAGIC
# MAGIC ### Mac
# MAGIC ```bash
# MAGIC # Install Databricks CLI
# MAGIC brew install databricks/tap/databricks
# MAGIC
# MAGIC # Configure profile
# MAGIC databricks configure --profile upsc
# MAGIC # Host: https://adb-7405615460529826.6.azuredatabricks.net
# MAGIC # Token: dapi-xxxxx (your PAT)
# MAGIC
# MAGIC # Verify
# MAGIC databricks serving-endpoints list --profile upsc --output json | python3 -c "
# MAGIC import sys,json
# MAGIC for e in json.load(sys.stdin):
# MAGIC     if 'claude' in e['name']: print(f\"  âœ… {e['name']}\")
# MAGIC "
# MAGIC
# MAGIC # Install Python SDKs (for Methods 4 & 5)
# MAGIC pip install openai anthropic
# MAGIC ```
# MAGIC
# MAGIC ### Windows PowerShell
# MAGIC ```powershell
# MAGIC # Install via winget
# MAGIC winget install Databricks.DatabricksCLI
# MAGIC
# MAGIC # Configure (already done as DEFAULT)
# MAGIC databricks configure
# MAGIC # Host: https://adb-7405615460529826.6.azuredatabricks.net
# MAGIC # Token: dapi-xxxxx
# MAGIC
# MAGIC # Verify
# MAGIC databricks serving-endpoints list --output json
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 1ï¸âƒ£ Databricks CLI Direct (Simplest â€” Zero Code)
# MAGIC ```bash
# MAGIC # Basic question
# MAGIC databricks serving-endpoints query --profile upsc \
# MAGIC   --name databricks-claude-sonnet-4-6 \
# MAGIC   --input '{"messages": [{"role": "user", "content": "Explain Article 370 for UPSC"}], "max_tokens": 1000}'
# MAGIC
# MAGIC # With system prompt (UPSC expert mode)
# MAGIC databricks serving-endpoints query --profile upsc \
# MAGIC   --name databricks-claude-sonnet-4-6 \
# MAGIC   --input '{"messages": [{"role": "system", "content": "UPSC CSE expert. Cite articles, cases, dates."}, {"role": "user", "content": "Compare DPSP vs Fundamental Rights"}], "max_tokens": 2000}'
# MAGIC
# MAGIC # Use Opus for deep reasoning
# MAGIC databricks serving-endpoints query --profile upsc \
# MAGIC   --name databricks-claude-opus-4-6 \
# MAGIC   --input '{"messages": [{"role": "user", "content": "Write a 250-word Mains answer on judicial activism"}], "max_tokens": 2000}'
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 2ï¸âƒ£ curl + OpenAI-Compatible API
# MAGIC ```bash
# MAGIC # Step 1: Get token
# MAGIC # Mac:
# MAGIC export DB_TOKEN=$(databricks auth token --profile upsc \
# MAGIC   | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")
# MAGIC
# MAGIC # Windows PowerShell:
# MAGIC # $DB_TOKEN = (databricks auth token | ConvertFrom-Json).access_token
# MAGIC
# MAGIC # Step 2: Query Sonnet
# MAGIC curl -s https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints/databricks-claude-sonnet-4-6/invocations \
# MAGIC   -H "Authorization: Bearer $DB_TOKEN" \
# MAGIC   -H "Content-Type: application/json" \
# MAGIC   -d '{
# MAGIC     "messages": [
# MAGIC       {"role": "system", "content": "UPSC expert tutor"},
# MAGIC       {"role": "user", "content": "What is GST Council? Constitutional basis?"}
# MAGIC     ],
# MAGIC     "max_tokens": 1000
# MAGIC   }' | python3 -m json.tool
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 3ï¸âƒ£ curl + Anthropic-Native API
# MAGIC ```bash
# MAGIC # Uses Claude's native message format (same token)
# MAGIC curl -s https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints/databricks-claude-sonnet-4-6/anthropic/v1/messages \
# MAGIC   -H "Authorization: Bearer $DB_TOKEN" \
# MAGIC   -H "Content-Type: application/json" \
# MAGIC   -H "anthropic-version: 2023-06-01" \
# MAGIC   -d '{
# MAGIC     "model": "databricks-claude-sonnet-4-6",
# MAGIC     "max_tokens": 1000,
# MAGIC     "messages": [{"role": "user", "content": "Explain panchayati raj 73rd/74th amendments"}]
# MAGIC   }'
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 4ï¸âƒ£ Python â€” OpenAI SDK (save as `upsc_ask.py`)
# MAGIC ```python
# MAGIC from openai import OpenAI
# MAGIC import subprocess, json, sys
# MAGIC
# MAGIC # Auto-fetch token from CLI
# MAGIC token = json.loads(subprocess.run(
# MAGIC     ["databricks", "auth", "token", "--profile", "upsc"],
# MAGIC     capture_output=True, text=True
# MAGIC ).stdout)["access_token"]
# MAGIC
# MAGIC client = OpenAI(
# MAGIC     api_key=token,
# MAGIC     base_url="https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints"
# MAGIC )
# MAGIC
# MAGIC question = " ".join(sys.argv[1:]) if len(sys.argv) > 1 else input("UPSC Query: ")
# MAGIC
# MAGIC response = client.chat.completions.create(
# MAGIC     model="databricks-claude-sonnet-4-6",
# MAGIC     messages=[
# MAGIC         {"role": "system", "content": "UPSC CSE expert. Be precise, cite specifics."},
# MAGIC         {"role": "user", "content": question}
# MAGIC     ],
# MAGIC     max_tokens=2000, temperature=0.7
# MAGIC )
# MAGIC print(response.choices[0].message.content)
# MAGIC ```
# MAGIC ```bash
# MAGIC # Usage:
# MAGIC python3 upsc_ask.py Explain Article 21 right to privacy
# MAGIC python3 upsc_ask.py  # interactive mode
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 5ï¸âƒ£ Python â€” Anthropic SDK (native Claude)
# MAGIC ```python
# MAGIC import anthropic, subprocess, json, sys
# MAGIC
# MAGIC token = json.loads(subprocess.run(
# MAGIC     ["databricks", "auth", "token", "--profile", "upsc"],
# MAGIC     capture_output=True, text=True
# MAGIC ).stdout)["access_token"]
# MAGIC
# MAGIC client = anthropic.Anthropic(
# MAGIC     base_url="https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints/databricks-claude-sonnet-4-6/anthropic",
# MAGIC     api_key=token
# MAGIC )
# MAGIC
# MAGIC response = client.messages.create(
# MAGIC     model="databricks-claude-sonnet-4-6",
# MAGIC     max_tokens=2000,
# MAGIC     messages=[{"role": "user", "content": " ".join(sys.argv[1:])}]
# MAGIC )
# MAGIC print(response.content[0].text)
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 6ï¸âƒ£ Claude Code + MCP (Already Configured!)
# MAGIC ```bash
# MAGIC # Just open Claude Code (uses your Pro $20/mo)
# MAGIC claude
# MAGIC
# MAGIC # Then ask naturally â€” MCP servers auto-connect:
# MAGIC > Query today's CA stories from Databricks and summarize for Mains
# MAGIC > Search knowledge graph for Article 21 connections  
# MAGIC > What Telugu PYQ patterns exist for Kavitrayam?
# MAGIC > Run the healing embed pipeline for any missing chunks
# MAGIC
# MAGIC # Your 6 MCP servers:
# MAGIC #   SQL â†’ query any table
# MAGIC #   Vector Search â†’ semantic search 80,800 chunks
# MAGIC #   UC Functions â†’ search_knowledge_graph()
# MAGIC #   Genie UPSC â†’ natural language data queries
# MAGIC #   Genie Telugu â†’ Telugu optional queries
# MAGIC #   MLflow â†’ model experiments
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## 7ï¸âƒ£ Shell Alias (Add to `~/.zshrc` for instant access)
# MAGIC ```bash
# MAGIC # Add these to ~/.zshrc (Mac) or PowerShell $PROFILE (Windows)
# MAGIC
# MAGIC # Quick UPSC question
# MAGIC alias upsc='f(){ databricks serving-endpoints query --profile upsc --name databricks-claude-sonnet-4-6 --input "{\"messages\": [{\"role\": \"system\", \"content\": \"UPSC expert\"}, {\"role\": \"user\", \"content\": \"$*\"}], \"max_tokens\": 2000}"; }; f'
# MAGIC
# MAGIC # Deep reasoning with Opus
# MAGIC alias upsc-deep='f(){ databricks serving-endpoints query --profile upsc --name databricks-claude-opus-4-6 --input "{\"messages\": [{\"role\": \"user\", \"content\": \"$*\"}], \"max_tokens\": 3000}"; }; f'
# MAGIC
# MAGIC # After adding, reload:
# MAGIC source ~/.zshrc
# MAGIC ```
# MAGIC ```bash
# MAGIC # Now just type from anywhere:
# MAGIC upsc Explain Article 370 vs 35A
# MAGIC upsc Generate 5 MCQs on Indian economy
# MAGIC upsc-deep Write a complete essay on judicial independence
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Windows PowerShell Equivalents
# MAGIC ```powershell
# MAGIC # Add to $PROFILE (run: notepad $PROFILE)
# MAGIC function upsc {
# MAGIC     $q = $args -join ' '
# MAGIC     $body = @{messages=@(@{role='system';content='UPSC expert'},@{role='user';content=$q});max_tokens=2000} | ConvertTo-Json -Depth 3
# MAGIC     $token = (databricks auth token | ConvertFrom-Json).access_token
# MAGIC     $r = Invoke-RestMethod -Uri 'https://adb-7405615460529826.6.azuredatabricks.net/serving-endpoints/databricks-claude-sonnet-4-6/invocations' -Method POST -Headers @{Authorization="Bearer $token";'Content-Type'='application/json'} -Body $body
# MAGIC     $r.choices[0].message.content
# MAGIC }
# MAGIC
# MAGIC # Usage:
# MAGIC upsc "Explain panchayati raj"
# MAGIC ```
# MAGIC
# MAGIC ---
# MAGIC
# MAGIC ## Quick Reference Card
# MAGIC | What | Command |
# MAGIC |---|---|
# MAGIC | Quick question (Mac) | `upsc Explain Article 21` |
# MAGIC | Quick question (Win) | `upsc "Explain Article 21"` |
# MAGIC | Deep reasoning | `upsc-deep Write essay on federalism` |
# MAGIC | List Claude models | `databricks serving-endpoints list --profile upsc` |
# MAGIC | Interactive Python | `python3 upsc_ask.py` |
# MAGIC | Claude Code + data | `claude` (then ask naturally) |
# MAGIC | Obsidian AI chat | Cmd+Shift+C in Obsidian |

# COMMAND ----------

# DBTITLE 1,ðŸ“ Notebook Summary
# MAGIC %md
# MAGIC ---
# MAGIC ## ðŸ“ What This Notebook Covers
# MAGIC
# MAGIC | # | Cell | What It Does | ai_query Model |
# MAGIC |---|---|---|---|
# MAGIC | 2 | CA Story Analysis | Latest story â†’ GS mapping + model answer + MCQ | Sonnet 4.6 |
# MAGIC | 3 | Prelims MCQ Gen | Random Polity chunks â†’ 3 exam-quality MCQs | Sonnet 4.6 |
# MAGIC | 4 | KG Interlinking | Most connected entity â†’ cross-GS paper answer | Sonnet 4.6 |
# MAGIC | 5 | Telugu Evaluation | Random Telugu chunk â†’ PYQ question + rubric | Sonnet 4.6 |
# MAGIC | 6 | Trap Detector | Weekly traps â†’ flashcards + counter-MCQs | Sonnet 4.6 |
# MAGIC | 7 | Essay Predictions | Trending themes â†’ 2027 topic predictions | Sonnet 4.6 |
# MAGIC | 8 | Mains RAG | KB chunks + Sonnet â†’ complete 15-mark answer | Sonnet 4.6 |
# MAGIC
# MAGIC **All cells use `ai_query()` SQL function â€” no Python SDK needed. Just Run All.**
# MAGIC
# MAGIC ### Model Strategy Recap
# MAGIC * **NB6 CA Pipeline (7AM):** Opus 4.6 â€” deep 2-pass reasoning
# MAGIC * **Everything else:** Sonnet 4.6 â€” 90% quality, 3x cheaper
# MAGIC * **Telegram Bot:** Llama 3.3 70B â€” cheapest, scale-to-zero
# MAGIC * **Obsidian Copilot:** Opus 4.6 â€” 1M context for vault analysis
# MAGIC * **CLI queries:** Sonnet 4.6 via `upsc` alias